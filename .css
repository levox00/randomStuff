/* ==========================================================================
   1. ИМПОРТЫ
   ========================================================================== */
 
@import url("https://eject37.github.io/DiscordTheme/BetterFont.css");
@import url("https://eject37.github.io/DiscordTheme/HideTrash.css");
 
 
/* ==========================================================================
   2. ПЕРЕМЕННЫЕ
   ========================================================================== */
 
:root {
    /* === ЕДИНСТВЕННАЯ ПЕРЕМЕННАЯ ДЛЯ ТОНИРОВАНИЯ ВСЕЙ ТЕМЫ === */
    --tint-rgb: 255, 255, 255;   /* базовый оттенок стеклянных элементов */

    /* === ГЛОБАЛЬНЫЕ НАСТРОЙКИ (меняйте только их) === */
    --glass-opacity: 1;          /* 0 = полностью прозрачно, 1 = исходная непрозрачность */
    --bg-tint: transparent;      /* цветная заливка поверх гифки (например, rgba(255,0,0,0.15)) */
    --text-color: #ffffff;       /* основной цвет текста */

    /* Фон */
    --app-bg: url(https://media4.giphy.com/media/v1.Y2lkPTc5MGI3NjExbDR5cmR6ZjYydWxtYmpwc3pwOHBkNmZjbnRldHAxYzQxZDNsbmw1dSZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/3ov9jWPi1AH2XUfI6Q/giphy.gif);
    --blur-bg: 80px;
    --blur-other: 30px;
    --brightness-bg: 0.5;
 
    /* Общие стили — теперь прозрачность умножается на --glass-opacity */
    --color-main: rgba(var(--tint-rgb), calc(0.1 * var(--glass-opacity)));
    --border-main: 1px solid rgba(var(--tint-rgb), calc(0.1 * var(--glass-opacity)));
    --boxshadow: inset 0 0 10px rgba(var(--tint-rgb), calc(0.1 * var(--glass-opacity)));
}
 
 
/* ==========================================================================
   3. ФОН ПРИЛОЖЕНИЯ
   ========================================================================== */
 
body {
    background: var(--app-bg) !important;
    background-position: 15% !important;
    background-size: 105% 105% !important;
    color: var(--text-color);        /* ← применяем настраиваемый цвет текста */
}

/* --- Цветная тонировка фона (управляется через --bg-tint) --- */
body::after {
    content: "";
    position: fixed;
    inset: 0;
    background: var(--bg-tint);
    pointer-events: none;
    z-index: 0;
}
 
/* Размытие на фоне */
.bg__960e4 {
    backdrop-filter: blur(var(--blur-bg)) brightness(var(--brightness-bg));
}
 
 
/* ==========================================================================
   4. БАЗОВАЯ ТЕМА (visual-refresh override)
   ========================================================================== */
 
.visual-refresh .theme-darker,
.visual-refresh.theme-darker {
    --background-base-lowest: transparent;
    --bg-base-tertiary: transparent;
    --background-tertiary: transparent;
    --background-secondary: transparent;
    --background-primary: transparent;
    --background-base-lower: transparent;
    --custom-channel-members-bg: transparent;
    --background-base-low: transparent;
 
    --bg-brand: rgba(var(--tint-rgb), calc(0.2 * var(--glass-opacity)));
    --background-code: rgba(var(--tint-rgb), calc(0.05 * var(--glass-opacity)));
    --button-filled-brand-background: rgba(var(--tint-rgb), calc(0.1 * var(--glass-opacity)));
    --button-filled-brand-background-hover: rgba(var(--tint-rgb), calc(0.2 * var(--glass-opacity)));
    --background-surface-high: rgba(var(--tint-rgb), calc(0.15 * var(--glass-opacity)));
    --background-secondary-alt: rgba(var(--tint-rgb), calc(0.1 * var(--glass-opacity)));
    --background-modifier-hover: rgba(var(--tint-rgb), calc(0.15 * var(--glass-opacity)));
    --background-mod-subtle: rgba(var(--tint-rgb), calc(0.15 * var(--glass-opacity)));
    --card-primary-bg: rgba(var(--tint-rgb), calc(0.15 * var(--glass-opacity)));
    --bg-base-secondary: rgba(var(--tint-rgb), calc(0.15 * var(--glass-opacity)));
 
    --brand-500: rgba(91, 103, 234, 0.4);            /* акцент не меняем */
    --text-feedback-positive: rgba(var(--tint-rgb), calc(0.65 * var(--glass-opacity)));
    --green-360: rgba(var(--tint-rgb), calc(0.65 * var(--glass-opacity)));
}
 
 
/* ==========================================================================
   5. ЗВОНКИ И ГОЛОСОВЫЕ КАНАЛЫ
   ========================================================================== */
 
.visual-refresh .root_bfe55a {
    background: transparent;
}
 
.callContainer_cb9592 {
    background: transparent;
    box-shadow: inset 0 0 10px rgba(var(--tint-rgb), calc(0.05 * var(--glass-opacity)));
    border-radius: 10px;
    border: var(--border-main);
    margin-left: 10px;
    margin-right: 10px;
    margin-top: 10px;
}
 
.wrapper_cb9592.minimum_cb9592 {
    background: transparent !important;
}
 
.gradientContainer_bfe55a {
    background-image: linear-gradient(
        rgba(var(--tint-rgb), calc(0.04 * var(--glass-opacity))) 0%,
        rgba(0, 0, 0, 0) 50%
    );
}
 
.callContainer_cb9592:has(.actionRow__6981d) {
    margin: unset;
    border-radius: unset;
    border: unset;
    box-shadow: unset;
}
 
.chat_f75fb0:has(.actionRow__6981d) {
    border: unset !important;
}
 
.chat_f75fb0:has(.actionRow__6981d) .form_f75fb0 {
    margin: unset !important;
}
 
.border__2f4f7.speaking__2f4f7 {
    box-shadow: 0px 0px 30px 10px rgba(var(--tint-rgb), calc(0.2 * var(--glass-opacity))) inset;
}
 
 
/* ==========================================================================
   6. КОНТЕКСТНОЕ МЕНЮ И ПОПАПЫ
   ========================================================================== */
 
.menu_c1e9c4 {
    background: rgba(0, 0, 0, 0.158);
    border: var(--border-main);
}
 
.menu_c1e9c4::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    backdrop-filter: blur(var(--blur-other));
    pointer-events: none;
    z-index: -1;
    border-radius: 8px;
}
 
#image-context[class*="c1e9c47c23f12ca3-menu"] {
    backdrop-filter: blur(var(--blur-other));
}
 
 
/* ==========================================================================
   7. ТУЛТИПЫ
   ========================================================================== */
 
.tooltip_c36707 {
    background: rgba(0, 0, 0, 0.15) !important;
    backdrop-filter: blur(var(--blur-other));
    box-shadow: 0 0 10px rgba(0, 0, 0, 1);
}
 
.tooltipPrimary_c36707 .tooltipPointer_c36707 {
    display: none;
}
 
.visual-refresh .wrapper_f7ecac {
    background: transparent;
    backdrop-filter: blur(var(--blur-other));
    border: var(--border-main);
}
 
.reactionTooltip_b49891 {
    background: rgba(var(--tint-rgb), 0) !important;
    backdrop-filter: blur(var(--blur-other));
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
}
 
 
/* ==========================================================================
   8. ПРОФИЛИ
   ========================================================================== */
 
.banner__68edb {
    background: rgba(var(--tint-rgb), calc(0.05 * var(--glass-opacity))) !important;
}
 
.user-profile-modal,
.root__24502,
.inner_c0bea0 {
    background: transparent !important;
    backdrop-filter: blur(10px);
}
 
.outer_c0bea0,
.inner_c0bea0 {
    background: rgba(var(--tint-rgb), calc(0.05 * var(--glass-opacity))) !important;
}
 
.outer_ab8609 {
    background: transparent;
}
 
.accountProfileCard__1fed1 {
    border: var(--border-main);
}
 
:where(.overlay_c0bea0) {
    background: rgba(var(--tint-rgb), calc(0.1 * var(--glass-opacity)));
    border: var(--border-main) !important;
}
 
 
/* ==========================================================================
   9. ЧАТ
   ========================================================================== */
 
.channelBottomBarArea_f75fb0 {
    backdrop-filter: blur(var(--blur-other));
}
 
.channelTextArea_f75fb0 {
    background: transparent !important;
}
 
.scrollableContainer__74017 {
    background: rgba(var(--tint-rgb), calc(0.03 * var(--glass-opacity))) !important;
    border: var(--border-main);
    box-shadow: var(--boxshadow);
}
 
.hoverButtonGroup__06ab4 {
    background: rgba(var(--tint-rgb), calc(0.1 * var(--glass-opacity)));
    border: var(--border-main) !important;
    backdrop-filter: blur(var(--blur-other));
    box-shadow: 0px 0px 10px 5px rgba(0, 0, 0, 0.3);
}
 
 
/* ==========================================================================
   10. ЗАКРЕПЛЁННЫЕ СООБЩЕНИЯ
   ========================================================================== */
 
.visual-refresh .messagesPopoutWrap_e8b59c,
.visual-refresh .messageGroupWrapper_e8b59c {
    background: rgba(var(--tint-rgb), calc(0.1 * var(--glass-opacity)));
    backdrop-filter: blur(10px);
}
 
.visual-refresh .header_e8b59c {
    background: rgba(0, 0, 0, 0.3);
    border-bottom: var(--border-main) !important;
}
 
.visual-refresh .file__0ccae {
    background: rgba(0, 0, 0, 0.3);
}
 
.visual-refresh .jumpButton_e8b59c {
    background: rgba(0, 0, 0, 0.35);
    box-shadow: 0px 0px 10px 5px rgba(0, 0, 0, 0.3);
}
 
 
/* ==========================================================================
   11. ПАНЕЛИ УПРАВЛЕНИЯ
   ========================================================================== */
 
.visual-refresh .panels__5e434 {
    background: rgba(var(--tint-rgb), calc(0.03 * var(--glass-opacity))) !important;
    border: var(--border-main) !important;
    backdrop-filter: blur(var(--blur-bg));
    box-shadow: var(--boxshadow);
}
 
.panel__5dec7 {
    background: transparent !important;
}
 
.buttonSection__1405b {
    background:
        radial-gradient(90.42% 100% at 50% 100%, rgba(19, 18, 18, 0.5) 60%, transparent 100%),
        linear-gradient(270deg, rgba(40, 39, 43, 0.5) 0, rgba(29, 29, 32, 0.5) 100%) !important;
    backdrop-filter: blur(10px);
}
 
.disconnect_f1ceac {
    background:
        radial-gradient(90.42% 100% at 50% 100%, rgba(19, 18, 18, 0.5) 60%, transparent 100%),
        linear-gradient(270deg, rgba(122, 71, 90, 0.5) 0, rgba(122, 71, 86, 0.5) 100%) !important;
}
 
.buttonSection__1405b,
.disconnect_f1ceac {
    border: none;
}
 
 
/* ==========================================================================
   12. КНОПКИ И ИНТЕРАКТИВНЫЕ ЭЛЕМЕНТЫ
   ========================================================================== */
 
button.vc-ranb-button.button__201d5.lookFilled__201d5.sizeMin__201d5.grow__201d5 {
    border: none;
}
 
.container__3f21e.checked__3f21e {
    background-color: rgba(var(--tint-rgb), calc(0.24 * var(--glass-opacity))) !important;
}
 
:where(.visual-refresh) .checkbox_f525d3.checked_f525d3 {
    background:
        radial-gradient(90.42% 100% at 50% 100%, rgba(226, 135, 135, 0.5) 60%, transparent 100%),
        linear-gradient(270deg, rgba(31, 17, 119, 0.5) 0, rgba(130, 110, 238, 0.5) 100%) !important;
    border: none;
}
 
 
/* ==========================================================================
   13. ПРОГРЕСС-БАРЫ И ИНДИКАТОРЫ
   ========================================================================== */
 
.barFill_a562c8 {
    background:
        radial-gradient(ellipse farthest-corner at right bottom,
            #fedb37 0%, #FDB931 8%, #9f7928 30%, #8a6e2f 40%, transparent 80%),
        radial-gradient(ellipse farthest-corner at left top,
            #ffffff 0%, #ffffac 8%, #d1b464 25%, #5d4a1f 62.5%, #5d4a1f 100%);
    background-color: transparent !important;
    border: none;
}
 
 
/* ==========================================================================
   14. ПРОЧЕЕ
   ========================================================================== */
 
.artContainer_a55fdc {
    display: none !important;
}
 
.border_a55fdc,
.modalFooter_a55fdc {
    background-color: rgba(var(--tint-rgb), calc(0.05 * var(--glass-opacity))) !important;
    backdrop-filter: blur(20px);
}
 
.visual-refresh .container_a55fdc {
    background:
        radial-gradient(90.42% 100% at 50% 100%, rgba(0, 0, 0, 0) 60%, transparent 100%),
        linear-gradient(270deg, rgba(27, 27, 29, 0.5) 0, rgba(35, 35, 39, 0.5) 100%);
}
 
.quickSelectPopout_ebaca5 {
    background: rgba(var(--tint-rgb), calc(0.15 * var(--glass-opacity))) !important;
    backdrop-filter: blur(10px);
}
 
.root__49fc1 {
    background: rgba(var(--tint-rgb), calc(0.15 * var(--glass-opacity)));
    backdrop-filter: blur(10px);
}
 
.footer__49fc1 {
    background: transparent;
}
 
.container_abd9a8{
    background: rgba(var(--tint-rgb), calc(0.05 * var(--glass-opacity)));
    backdrop-filter: blur(10px);
}
